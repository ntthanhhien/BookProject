<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE logback>
<configuration debug="true">
	<include
		resource="org/springframework/boot/logging/logback/defaults.xml" />

	<!-- コンソール出力 -->
	<appender name="STDOUT"
		class="ch.qos.logback.core.ConsoleAppender">
		<target>System.out</target>
		<encoder>
			<charset>UTF-8</charset>
			<pattern>%d{yyyy/MM/dd HH:mm:ss} %-5level [%thread] - %msg%n</pattern>
		</encoder>
	</appender>


	<!-- アプリケーションログのファイル書き出し -->
	<appender name="APPLIATION_LOG"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<!--ファイル名 -->
		<file>/var/log/tomcat/sample.log</file>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>/var/log/tomcat/sample-%d{yyyy-MM-dd}.log.zip</fileNamePattern>
			<maxHistory>3</maxHistory>
		</rollingPolicy>
		<encoder>
			<charset>UTF-8</charset>
			<pattern>%d{yyyy/MM/dd HH:mm:ss} %-5level [%thread] - %msg%n</pattern>
		</encoder>
	</appender>

	<springProfile name="development,sample">
		<root level="DEBUG">
			<appender-ref ref="STDOUT" />
		</root>
		<logger name="org.springframework.boot.web.servlet.ServletContextInitializerBeans" level="TRACE" additivity="false">
			<appender-ref ref="STDOUT" />
		</logger>
		<logger name="vn.com.vti" level="DEBUG" additivity="false">
			<appender-ref ref="STDOUT" />
		</logger>
	</springProfile>

	<springProfile name="stage">
		<root level="DEBUG">
			<appender-ref ref="APPLIATION_LOG" />
		</root>
	</springProfile>

</configuration>